
%h1= @category.name 
= link_to "Edit Category", edit_category_path(@category), class: "btn btn-default"
= link_to "Delete", category_path(@category), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default"
- @articles.each do |article| 

  %h3= link_to  article.title, article  
  %p= article.content 
 

- if (@articles.empty?)  
  %p Sorry, There are no articles in this Category 

= link_to  "Back to Categories", categories_path, class: "btn btn-default" 




  